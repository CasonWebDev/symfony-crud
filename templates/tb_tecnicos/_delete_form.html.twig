<form method="post" action="{{ path('tb_tecnicos_delete', {'id': tb_tecnico.getId}) }}" onsubmit="return confirm('Tem certeza que deseja deletar esse item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tb_tecnico.getId) }}">
    <button class="btn"><i class="delete-icon fa fa-trash" aria-hidden="true" title="Deletar"></i></button>
</form>
